# PRACTICE PROJECT

Learning GIT Python Foundation

## GIT COMMANDS:
```
git config --global user.name
git config --global user.name "your_name"
git config --global user.name.email "your_email"

git init
git status
git add .
git commit -m "BBR: Initial Setup"
git log --oneline

git remote add origin http://github_address
git push origin master
git pull origin master
```

## Python Foundation:

- Primitive Variables
- Function
- Object
- Class 
- Class deep dive
- Opeation & Conditions
- Loops
- .... 